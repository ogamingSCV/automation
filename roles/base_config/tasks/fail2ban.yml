---
- name: Install fail2ban
  ansible.builtin.apt:
    name: fail2ban
    state: present

- name: Copy config
  ansible.builtin.copy:
    src: jail.local
    dest: "/etc/fail2ban/jail.local"
    owner: "root"
    group: "root"
    mode: '0640'
  notify: Restart fail2ban
